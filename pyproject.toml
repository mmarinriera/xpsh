[build-system]
build-backend = "setuptools.build_meta"
requires = [ "setuptools>=80" ]

[project]
name = "xpsh"
version = "0.10.0"
description = "Expense sharing tool."
readme = "README.md"
license-files = [ "LICENSE" ]
requires-python = ">=3.13"
classifiers = [
  "Programming Language :: Python :: 3 :: Only",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
]
dependencies = [
  "click>=8.4",
  "filelock>=3.29",
  "plotext==5.2.8",
  "rich>=15",
  "streamlit>=1.57",
]
urls.Repository = "https://github.com/mmarinriera/xpsh"
scripts.xpsh = "xpsh.scripts.main:xpsh"

[dependency-groups]
dev = [
  "coverage[toml]>=7.13.4",
  "pytest>=9",
  "pytest-lazy-fixtures>=1.4",
]

[tool.setuptools]
packages.find.where = [ "src" ]
include-package-data = true

[tool.uv]
required-version = ">=0.11.0"

[tool.ruff]
target-version = "py313"
line-length = 120
fix-only = true
lint.select = [ "ALL" ]
lint.ignore = [ "COM812", "D203", "D212" ]
lint.isort.force-single-line = true

[tool.mypy]
ignore_missing_imports = true
disallow_any_generics = true
disallow_untyped_calls = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
warn_redundant_casts = true
warn_return_any = true
warn_unreachable = true
strict_equality = true
warn_unused_configs = true
no_implicit_optional = true
no_implicit_reexport = true
show_error_codes = true

[tool.pytest]
addopts = [ "-s", "-vvv" ]
log_cli = true
log_cli_level = "DEBUG"
minversion = "9.0"
testpaths = [
  "tests",
]

[tool.coverage]
run.branch = true
run.source = [ "xpsh" ]
# Regexes for lines to exclude from consideration
report.exclude_also = [
  # Don't complain about abstract methods, they aren't run:
  "@(abc\\.)?abstractmethod",
  # Don't complain about missing debug-only code:
  "def __repr__",
  "if __name__ == .__main__.:",
  # Don't complain if non-runnable code isn't run:
  "if 0:",
  "if self\\.debug",
  # Don't complain if tests don't hit defensive assertion code:
  "raise AssertionError",
  "raise NotImplementedError",
]
report.ignore_errors = true
html.directory = ".coverage_html_report"

[tool.semantic_release]
allow_zero_version = true
version_variables = [ "src/xpsh/__init__.py:VERSION" ]
version_toml = [ "pyproject.toml:project.version" ]
build_command = '''    uv lock --upgrade-package "$PACKAGE_NAME"
    git add uv.lock
    uv build
'''
commit_message = "chore(release): version {version}"
publish.upload_to_vcs_release = false
changelog.default_templates.changelog_file = "CHANGELOG.md"
branch = "main"
